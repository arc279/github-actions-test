name: "test"

on:
  push:
  workflow_dispatch:

env:
  ENV: develop
  AWS_DEFAULT_REGION: ap-northeast-1

jobs:
  test-job1:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository code
        uses: actions/checkout@v3

      - name: step1
        run: |
          curl -V
          pwd
          ls -la
          echo ${{ env.AWS_DEFAULT_REGION }}
          echo ${{ env.ENV }}
          echo event number: ${{github.event.number}}
          echo event name: ${{ github.event_name }}
          echo event type: ${{ github.event.action }}
          echo merged: ${{ github.event.pull_request.merged }}

          echo '2' ${{ github.ref }}
          echo '3' ${{ github.ref_name }}
          echo '4' ${{ github.head_ref }}
          echo '5' ${{ github.base_ref }}

      - name: step2
        uses: ./.github/actions/assume-role
        with:
          aws_account_id: 123456789012

      - name: step3
        run: |
          echo ${GITHUB_ENV}
          cat ${GITHUB_ENV}
          echo ${AWS_ACCESS_KEY_ID}
          echo ${AWS_SECRET_ACCESS_KEY}
          echo ${AWS_SESSION_TOKEN}

  test-job2:
    uses: ./.github/workflows/reuse_test.yml

  test-job3:
    runs-on: ubuntu-latest

    steps:
      - name: Set the value
        id: step_one
        run: |
          echo "action_state=yellow" >> "$GITHUB_ENV"
          echo ${GITHUB_ENV}
          cat ${GITHUB_ENV}

      - name: Use the value
        id: step_two
        run: |
          echo "${{ env.action_state }}"
          echo ${action_state}
          echo ${GITHUB_ENV}
          cat ${GITHUB_ENV}

    # steps:
    #   - name: step1
    #     run: |
    #       echo '{"Credentials":{"AccessKeyId":"AAA","SecretAccessKey":"BBB","SessionToken":"CCC","Expiration":"2023-07-10T02:56:21+00:00"},"AssumedRoleUser":{"AssumedRoleId":"DDD","Arn":"EEE"}}' >/tmp/cred.json
    #       jq -r '"export AWS_ACCESS_KEY_ID=\(.Credentials.AccessKeyId)"' /tmp/cred.json >> ${GITHUB_ENV}
    #       jq -r '"export AWS_SECRET_ACCESS_KEY=\(.Credentials.SecretAccessKey)"' /tmp/cred.json >> ${GITHUB_ENV}
    #       jq -r '"export AWS_SESSION_TOKEN=\(.Credentials.SessionToken)"' /tmp/cred.json >> ${GITHUB_ENV}

    #       echo /tmp/cred.json
    #       cat /tmp/cred.json
    #       echo ${GITHUB_ENV}
    #       cat ${GITHUB_ENV}

    #   - name: step2
    #     run: |
    #       echo ${GITHUB_ENV}
    #       cat ${GITHUB_ENV}
    #       echo ${AWS_ACCESS_KEY_ID}
    #       echo ${AWS_SECRET_ACCESS_KEY}
    #       echo ${AWS_SESSION_TOKEN}
